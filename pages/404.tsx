
// component
import { Layout } from '../components/layout'

export default function NotFound(){

    return (
        <Layout title={ `404 page| Next.js + tailwind + keycloak Example`}>
            <div>what are you doing here?</div>
        </Layout>
    );
}
